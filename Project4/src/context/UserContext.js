import React from "react";
const UserContext = React.createContext();
export default UserContext;
//now we will wrap all the components by this UserContext
//so it will become provider i.e it will provide data
